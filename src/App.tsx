import React from "react";
import tw, { styled } from "twin.macro";
import Router from "./app/routes";

const App: React.FC = () => {
    return (
        <>
            <Router />
        </>
    );
};

export default App;
